{
  "version": 2,
  "builds": [
    {
      "src": "frontend/**",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "$LEADFLOW_BACKEND_URL/api/$1",
      "headers": {
        "Access-Control-Allow-Origin": "*"
      }
    },
    {
      "src": "/ws",
      "dest": "$LEADFLOW_BACKEND_URL/ws"
    },
    {
      "src": "/(.*)",
      "dest": "/frontend/$1"
    }
  ]
}
